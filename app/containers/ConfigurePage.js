// @flow
import React, { Component } from 'react';
import Configure from 'components/configure/Configure';

export default class ConfiguePage extends Component {
    render() {
        return (
            <Configure/>
        );
    }
}
