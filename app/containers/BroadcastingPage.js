// @flow
import React, { Component } from 'react';
import Broadcasting from 'components/broadcasting/Broadcasting';

export default class BroadcastingPage extends Component {
    render() {
        return (
            <Broadcasting/>
        );
    }
}
